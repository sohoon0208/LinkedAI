interface:
  display_name: "LinkedAI"
  short_description: "ASTRA HIGH plans; LUNA MAX executes; SOL HIGH verifies."
  default_prompt: "Use $linkedai: run LUNA RECON, compress a bounded PACKET, dispatch ASTRA HIGH for the plan, let LUNA MAX execute it, and have SOL HIGH independently verify the result. No approval pause or automatic AG stage; SOL alone can declare DONE."

interface:
  display_name: "LinkedAI"
  short_description: "ASTRA HIGH plans; LUNA MAX executes; SOL verifies in balance."
  default_prompt: "Use $linkedai: run LUNA RECON, compress a bounded PACKET, dispatch ASTRA HIGH for the plan, let LUNA MAX execute it, and have SOL use BALANCED independent verification by default (FULL for DEEP work). No approval pause or automatic AG stage; SOL alone can declare DONE."
